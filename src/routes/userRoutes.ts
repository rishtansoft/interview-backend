import { Router } from 'express';
import { login, createSuperadmin } from '../controllers/userController';

const router = Router();

// router.get('/', getUsers);
// router.post('/', createUser);

export default router;
